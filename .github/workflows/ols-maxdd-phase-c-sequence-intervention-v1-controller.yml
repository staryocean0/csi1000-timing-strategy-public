name: OLS MaxDD Phase C sequence intervention bounded controller

on:
  issues:
    types: [opened]

permissions:
  contents: read
  issues: read
  actions: write

concurrency:
  group: ols-maxdd-phase-c-sequence-intervention-controller-${{ github.event.issue.number }}
  cancel-in-progress: false

jobs:
  dispatch:
    if: >-
      github.repository == 'staryocean0/csi1000-timing-strategy-public' &&
      github.event_name == 'issues' &&
      github.event.action == 'opened' &&
      github.event.issue.user.login == github.repository_owner &&
      github.event.issue.title == 'controller: ols-maxdd-phase-c-sequence-intervention-v1'
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Dispatch preregistered bounded Phase C study
        shell: bash
        run: |
          set -euo pipefail
          gh api \
            --method POST \
            repos/staryocean0/csi1000-timing-strategy-public/actions/workflows/ols-maxdd-phase-c-sequence-intervention-v1.yml/dispatches \
            -f ref='cloud-workspace-v1'
          echo 'Approved OLS MaxDD Phase C workflow_dispatch submitted.'
