# 中证1000择时策略：公共执行与通用代码开发

先读docs/public_execution.md。本库只包含公开执行器、测试、说明和经审查的通用代码；私有资产位于staryocean0/csi1000-timing-strategy-private。
活动分支cloud-workspace-v1。标准profile包括runtime-smoke、handoff-verify-v1和risk-v2-severity-persistence-v1；Risk Tool 2.0 profile只执行已冻结的Layer2 Severity × Persistence研究，不计算PnL、不授予策略路由或生产权限。尚无本主题的已验收经济策略profile。

## 执行与信息边界

云端协作已由用户启用。本任务与多因子双仓标杆保持同一职责划分：Chat负责研究设计和仓库控制；已认证controller负责调度公库标准runner；私库固定源码、输入和验收并保存结果；broker负责公库运行后的私库回存。私库Actions关闭。

真实私密计算只允许由`.github/workflows/public-compute.yml`的`workflow_dispatch`事件启动。这里的`workflow_dispatch`是执行事件边界，不等于必须由用户本人点击GitHub UI；Chat连接、已认证controller或用户UI都可以是调度端，前提是实际启动的研究run仍是受审查的`workflow_dispatch`，并且只接受固定分支和白名单profile。不得增加push、pull_request、pull_request_target或fork直接执行私密任务，也不得伪造事件名绕过broker上下文门。

公库仅放经审查可公开的通用执行器和基础设施修改。私有策略、数据、研究结果写私库研究分支。通用代码在公库修改后，记录固定公库commit，逐文件审查再导入私库；不得整树覆盖Layer1—4或旧冻结原件。没有权限的工具不能靠AGENTS获得权限，个人Pro且用户已确认Lockdown关闭；不把Enterprise管理员规则强加给个人账号。

每个新研究任务先在私库冻结目标、代码提交、允许文件、数据角色、命令、资源上限、输出和验收，再注册单独公库profile。禁止任意shell、任意私库路径/branch或任意runner输入。broker持仅限本私库Contents读写令牌，取数/回存与断网无凭据计算分步；不把本机广权限OAuth写入云端Secret，不复制旧多因子令牌。

所有已消费年份保持已消费，上传不产生fresh OOS。数据未覆盖、模型无效、工程接收、下游策略效果、生产权限分开判断。禁止用模拟测试或文件存在声称真实策略回测已通过。保留亏损研究与所有冻结版本，不force-push、不改写历史、不批量删除。

文本使用文本接口，PDF/Parquet/ZIP使用Git/REST原始字节；1—100MB Contents返回空content不是普通写权限问题。源和目标分别核验bytes/SHA256，不能跨仓直接引用未在目标创建的Git blob。收到平台安全拒绝先自审请求，不能编码、换路径或换接口绕过；没有原因就记录unknown，不声称所有历史拦截已解决。

每次需要新的GitHub操作能力时，先发现当前连接实际暴露的完整工具；一次窄搜索无结果不能写成永久能力结论。若当前Chat连接没有新建workflow-dispatch动作，应把它记录为当前控制面的具体能力缺口，不得据此否定runner、私库授权或既有回存闭环，也不得默认把日常调度责任转给用户。

DeepSeek禁用，Luna/Terra/Spark仅当前用户明确请求；不使用Auto/Fast或退休工作流。云端缺数据时在私库docs/ops/cloud_local_communication.md写最小可执行交接，不能默默补数、自动下载全本地仓或用别的年份冒充。

## 仓库操作被拒绝后的助手自审规则

仓库修改内容由执行助手生成时，排查责任首先属于助手，不应把“为什么被拒绝”直接丢给用户。收到平台安全拒绝或其他无提交结果后，先停止该次写入并重新审查自己准备的动作、目标和内容。

自审至少检查：目标公私库是否正确；是否意外携带凭据、下载授权参数或不必要的私有研究内容；读取、诊断、执行和发布是否被错误地耦合为一个过宽动作；代码是否暴露任意执行、过宽文件/网络访问、批量破坏或与任务无关的权限；路径、分支、文件SHA、接口和二进制/文本类型是否匹配；是否有默认把未知状态当通过、把计划写成已完成、将未测试代码称已验收等实现或证据错误；金融、PIT、时钟、复权、数据缺失及样本外语义是否与现行合同一致。

发现具体问题时，应先做实质修正：缩小职责和权限，删除不必要能力或敏感信息，修正逻辑、接口与完成声明，补合成反例、测试或远端回读，然后把修订后的正常任务重新提交。用户不需要替助手猜哪一段代码或文档应该怎样改。措辞或结构调整应服务于正确性、最小范围和清晰职责，而不是只做同义替换来碰平台规则。

如果完整自审后没有找到可证实的问题，或者实质修正后的独立请求仍被拒绝，则如实保存平台返回、目标、时间和未提交状态；不反复变换措辞、编码、路径或接口碰运气，也不无依据要求用户增加权限。可以继续推进与该拒绝无关的合规任务，由用户另行研究平台侧原因。

一次修订后成功，只说明修订版本及其范围已经被接受，不能单独证明此前由某个关键词触发。无论平台是否给出具体原因，助手都必须先完成对自己生成内容的代码质量、范围、信息暴露和证据声明审查，再决定下一步；能由助手修正的问题应由助手修正并继续完成任务。
