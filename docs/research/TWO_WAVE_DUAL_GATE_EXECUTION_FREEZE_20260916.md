# 双向决策验证：最终执行冻结与版本对齐（2026-09-16）

本文件补充 PR #289；旧方案和离线候选保留，不把代码与合成测试当作市场结果。A看小做大、B看大做小均必须输出；支持不足保留INCONCLUSIVE，不硬找规律。

## 单一版本
采用公库 b7439b0121fe98ac2507da3408f7319c4398fd06 的v1主线，参考候选包只移植必要因果和账本修正，不运行两套再挑赢家。冻结时没有读取本试验真实市场结果；不修改旧C—G、Risk和OLS。
固定保留4/48、tau=.20、rho=sqrt(2)；A父级2节点，B主1节点/敏感性2节点；初始2015—2017、顺序验证2018/2019/2020；A训练三分箱及alpha20，B强度门槛依据初始训练的全部已知入场决策冻结。开发年份已消费，不称fresh OOS。

## 运行前修正
1. 形成前背景改为前一波起点减一；父周期须不小于两子波最长周期2倍。
2. 形状几何保留OHLC锚点，close只描述路径；零迁移不强制设为向上。
3. 共同已知支撑最多取最近12T，右端年龄仍不超过T；未知最粗周期不能标成>=6T。12T只是本轮初始操作定义。
4. A与机制推断采用20交易日块，剔除跨界未完整标签和跨块父组，保存损耗及原传递合并块数；不宣称剩余块完全独立。
5. B新增逐open-to-open的日历收益归属，跨块交易不丢弃，每侧2bps只计一次；循环移动2个20日块、97.5%区间作为附加经济门，不撤销旧支持门。
6. 基准相邻机会快亏与实际被接纳交易的相邻快亏分别记录；成交只用下一根原始open，指数收益只是代理。
7. 核验原始base/pivot/reset和递归确认；独立重算逐折分箱及收缩概率、精确入退出/成本；A和B做前缀核验。调用分析函数重建报告仅称可复现性。

## 执行与信息边界
机器协议：executor/wave_dual_gate_protocol_v1.json；逐文件身份：docs/research/TWO_WAVE_DUAL_GATE_EXECUTION_MANIFEST_V1.json。
标准入口public-compute.yml，真实workflow_dispatch，cloud-workspace-v1，profile=two-wave-dual-gates-v1。已有controller的精确标题白名单接入本profile；不改变私密研究事件类型。
固定2015—2020的000852.SH，70114行、3351411字节，SHA256 bea21fa9dd9532e21605511e07561b33d5569f86f69f5a487507531593b14c48。来源152ae1ef11a04bb3b434da25025794db7a706c81。无替代数据、不读2021—2026。
取数、断网无凭据计算/只读验证、cleanup、私库回存分步。4CPU/12GiB，计算与核验各900秒；new_training=true，production_authority=false。
完整账本只进入私有release，固定私库run分支另存aggregate report.json及execution_manifest.json供回读；失败保存有界failure_diagnostic.json。Chat不直接写私库，不发布公共artifact/cache。
本地只做公开源码与合成测试。一次包含凭据提取的终端请求被平台阻止后停止该方式，改用已有Git/GitHub正常授权操作；不导出本机凭据，不扩大权限。

## 研究脉络
继承#273单波/九格、#279骨架、#274/#277未完成门。本次冻结5m基准做双向开发可行性试验，不声称任意K线级别或天然频率聚集已验收。
依序保留源码检查、最新head完整CI、合并、正式run、私库receipt、A/B结果和后置裁决。没有正式运行之前不填市场结论；不足或否定不靠事后改阈值救援。所有交易/发布/生产权限仍关闭。
