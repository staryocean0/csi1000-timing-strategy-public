# State-conditional calibration v2 source registration

Issue #381 under #376/#353. This checkpoint freezes the deterministic **development-only** rule family after the label-blind v2 diagnostic object was frozen at `a0b71c19591a83bf2ee7cc24eaecaea367d0d3b3772a0e41c03d7c361b5fa659` and before any v2 feature × reference-label conditional inspection.

Validity uses separate training-only INVALID and VALID evidence rules. Jump magnitude/isolation alone cannot invalidate a case; sustained post-jump level holding can support VALID. Conflicts, missing evidence, and double-negative evidence remain `UNCERTAIN_VALIDITY`.

Morphology selects one preregistered normalized evidence axis using training years only and fits two non-crossing boundaries. Values between the developing and turning boundaries are explicitly `MORPHOLOGY_AMBIGUOUS`. Raw amplitude is not a calibration axis.

Dominance reuses the v1 frozen finite family and directions without redesign. The 2015–2020 LOYO that follows is iterative development evidence, not fresh OOS. No full-192 fit, R4, router, PnL, signal/trade, or production authority is authorized.
