# R1A Stage A integration branch

This branch is reserved for reviewed engineering changes. It must not add credentials, secret handling, arbitrary command execution, external writeback, or production authority.
